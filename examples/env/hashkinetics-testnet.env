CHAIN=hashkinetics-testnet
PORT=26660
RPC_URL=http://127.0.0.1:26000
# Optional reference for lag and same-height app_hash checks.
API_URL=https://rpc.hashkinetics.org
# Optional public SLH-DSA root identity: hex-encode validator.json's root_pk bytes.
# VALIDATOR=<96-character-public-root-hex>
